import React from "react";

/**
 * VRControls — Placeholder for VR teleport locomotion.
 * Currently disabled (XR wrapper removed for stability).
 * Will be re-enabled when VR headset support is wired back in.
 */
export function VRControls() {
  return null;
}
